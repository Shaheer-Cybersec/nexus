"""NEXUS feature modules.

Each module exposes a single ``run()`` entry point that the main menu
dispatches to. Modules are intentionally self-contained so they can also
be invoked directly from the command line.
"""
